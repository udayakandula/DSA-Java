class Solution {
    public boolean isSorted(ArrayList<Integer> nums) {
        //your code goes here
        boolean sort=true;
        for(int i=0;i<nums.size()-1;i++)
        {
            if(nums.get(i)>=nums.get(i+1))
            {
               sort=false;
               break;
            }
        }
        return sort;
    }
}
